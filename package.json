{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Jared Henderson <jared@netrivet.com>",
  "license": "MIT",
  "scripts": {
    "start": "./node_modules/.bin/netlify dev --functions ./functions",
    "build": "./node_modules/.bin/netlify functions:build --src ./functions",
    "deploy": "mkdir -p ./build && npm run build && npx netlify deploy --dir=build --functions=built-functions --prod"
  },
  "dependencies": {
    "netlify-cli": "^2.50.0",
    "node-fetch": "^2.6.0"
  }
}
